module.exports = {
    setupFilesAfterEnv: ['./jest.setup.js'],
    verbose: true,
    // collectCoverage: true,
    // coverageDirectory: 'coverage', // This is the default, but you can change it
    // coverageReporters: ['json', 'html', 'lcov'], // Specify desired report formats
    // // ... other Jest configurations
};